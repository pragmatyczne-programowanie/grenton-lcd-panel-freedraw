# Grenton Smartpanel configuration examples 

## Panel refersh

The script `sensors_freedraw_page` has to be set as "OnDraw" action in a page configuration of the panel.

## Icons

In the `icons` folder you can find 16x16 icons to customize the script. Replace `arrdowns` with desired icon name. 

Don't forget to copy selected icons to SD card of the panel.
